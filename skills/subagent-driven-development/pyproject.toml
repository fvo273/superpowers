[project]
name = "openrouter-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["openai>=1.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = ["pytest>=8.0.0"]
